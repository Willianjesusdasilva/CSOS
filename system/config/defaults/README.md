# Versioned CSOS defaults

Files in this directory are reproducible system defaults and belong to the
versioned `/system` tree. Do not write machine-specific discovery results here.

Machine-specific hardware facts and tuning baselines belong in:

```text
/data/config/hardware.csc
```
